services:
  postgres:
    image: synthesizedio/pagila:latest
    ports:
      - "5433:5432"
    environment:
      POSTGRES_PASSWORD: postgres
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      retries: 5
